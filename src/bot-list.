// src/bot-list.ts

// The "Wallet Drainers" - High volume AI crawlers
export const BAD_BOTS = [
  'GPTBot',             // OpenAI
  'ChatGPT-User',       // OpenAI
  'FacebookBot',        // Meta
  'Meta-ExternalAgent', // Meta
  'ClaudeBot',          // Anthropic
  'Bytespider',         // TikTok/ByteDance (Very aggressive)
  'Amazonbot',          // Amazon
  'PerplexityBot',      // Perplexity AI
  'CCBot',              // Common Crawl (Used to train AI)
  'anthropic-ai',
  'cohere-ai',
  'Omgilibot',
  'FacebookExternalHit'
];

// Whitelisted paths that should NEVER be blocked (e.g., Payment Webhooks)
export const DEFAULT_WHITELIST = [
  '/api/auth',
  '/api/webhooks/stripe',
  '/api/webhooks/lemon-squeezy',
  '/_next/static',
  '/favicon.ico'
];
